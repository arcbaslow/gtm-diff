# Offline documentation example

These are synthetic fixtures for `gtm-diff`. They contain no real account, customer or design data. Run the command in the root README's **Example output** section to reproduce the generated files. Figma uses `tests/fixtures/banking_app.json`; GTM uses the two exports in `test/fixtures/`.

`assets/screenshot.png` is a browser capture of the generated output. Markdown is displayed in a documentation viewer; the GTM HTML report is displayed directly. This preview is not a dashboard or a live account audit.
